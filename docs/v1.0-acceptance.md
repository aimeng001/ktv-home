# Home KTV v1.0 验收记录

状态：部分自动化验证已配置；真实设备、长时间运行和 NAS 只读验收待执行。

记录范围：T09 稳定性、CI、Release。本文只记录当前工作区能够证明的事实，不把未执行的长时间测试写成通过。

## 发布与 CI 基线

| 项目 | 当前配置/证据 | 状态 |
| --- | --- | --- |
| Backend CI | `.github/workflows/ci.yml` 的 `backend` job 在 `backend` 工作目录执行 `./mvnw --batch-mode test` | 已纳入 CI；本机本轮全量 260 tests：259 通过、0 失败、0 错误、1 个既有条件 skip |
| H5 CI | `h5` job 执行 `npm ci`、`npm test`、`npm run build` | 已纳入 CI；本机 35/35 通过，构建成功 |
| Android TV CI | `android-tv` job 执行 `testReleaseUnitTest`、`lintRelease` | 已纳入 CI；本机已验证 |
| Windows Player CI | `windows-player` job 使用 .NET 10，执行 Release tests 和 build | 已纳入 CI；本机 59/59 通过，构建 0 警告/0 错误 |
| Compose CI | `compose-config` job 对每个 `docker-compose*.yml` 执行 `docker compose config --quiet` | 已纳入 CI；本机使用 Docker Desktop CLI 逐个配置检查通过 |
| Windows x64 Release | Release workflow 发布 self-contained `win-x64` ZIP，并随包放入 mpv 前置说明 | 本机已验证；实际 tag 发布待验证；`mpv.exe` 需单独安装 |
| GHCR | Release workflow 的 `IMAGE_NAME` 为 `ghcr.io/aimeng001/ktv-home` | 已核实为 Fork 命名空间 |
| Android 正式签名 | Release workflow 继续使用 `KTV_TV_KEYSTORE_BASE64`、`KTV_TV_STORE_PASSWORD`、`KTV_TV_KEY_ALIAS`、`KTV_TV_KEY_PASSWORD` | 已保留；实际签名待 Release 运行验证 |
| LICENSE/版权 | `LICENSE` 仍存在且未被 T09 修改 | 已保留 |

## 本机验证

以下是本轮实际执行或明确未执行的命令。命令失败、工具缺失和人工验收不互相替代。

| 模块 | 命令 | 结果 |
| --- | --- | --- |
| H5 | `npm test`（工作目录 `h5`） | 通过：10 个测试文件、35 个测试 |
| H5 | `npm run build`（工作目录 `h5`） | 通过：Vite 转换 1811 个模块；产物写入 `backend/src/main/resources/static/m` |
| Windows | `dotnet test windows-player/HomeKtv.Windows.sln --no-restore -c Release` | 通过：59 个测试，0 失败/跳过 |
| Windows | `dotnet build windows-player/HomeKtv.Windows.sln --no-restore -c Release` | 通过：0 警告、0 错误 |
| Windows x64 包 | `dotnet publish ... -r win-x64` + `Compress-Archive` | 通过：已检查包含 `HomeKtv.Windows.exe` 和 `README-WINDOWS-PLAYER.md`，不含 PDB/日志或 `mpv.exe`；NuGet 漏洞源检查有网络警告 |
| Backend | `mvn -f backend/pom.xml test`（本机缓存 Maven 3.9.11，Docker Desktop 29.7.2） | 通过：260 tests、0 failures、0 errors、1 个既有条件 skip；8 个 Testcontainers 集成测试均已执行通过，Flyway 已执行到 v21 |
| Android | `./gradlew --no-daemon testReleaseUnitTest`、`lintRelease`、`assembleDebug`（在 `android-tv`） | 通过：release unit 56/56，lint、debug build 均成功；真实设备和正式签名仍待执行 |
| Docker/Compose | Docker Desktop CLI 的 `docker compose config`（六组配置） | 本机配置解析通过；仅有 `.docker/config.json` 访问警告，非 Compose 语法错误 |

本轮还执行了两个 workflow 的 YAML 解析和 Fork/签名/Windows 资产静态断言，均通过；这不等同于 GitHub Actions 云端运行或实际发布。

## 本次整改（R01–R05）

本轮按最小范围修复了已核实的问题：统一原唱切换能力判断、补齐新曲库目录监听、抑制过期播放错误广播、外部曲库管理操作默认隐藏/拒绝，以及将媒体与歌词 sidecar 快照分离。R05 使用 V20 加 V21 追加迁移；V21 将已存在的 `SMALLINT` 无损扩宽为实体对应的 `INTEGER`。歌词仅发生变化时，测试实测 `FFprobe=0`、完整 Hash=0，媒体文件字节保持不变；带有 `[ti]`/`[ar]` 身份标签的歌词变化则保守触发一次媒体探测。未修改源媒体文件。

R05 聚焦测试为 29 个：28 个通过、0 失败、1 个既有文件系统 identity 假设 skip；V21 与迁移安全测试为 3/3。完整 Backend 回归为 260 个：259 个通过、0 失败、0 错误、1 个既有 skip。

## 本轮 Backend 合规修复

| 行为 | 证据 | 状态 |
| --- | --- | --- |
| 搜索语种/分类 | SearchIntegrationTest 新增语种、tags 行为测试，GREEN 为 9/9 | 已自动验证 |
| 相对路径 | Flyway V19；LibraryScanIntegrationTest 查询得到相对文件名；Fast Index 在 FFprobe 前写入；根目录重新挂载后按唯一相对路径复用记录且不重新 probe | 已自动验证 |
| 旧记录兼容 | relative_path 允许 NULL，旧 song_files 行无需回填即可迁移 | 已由迁移定义保证；真实旧库升级待人工/CI 验证 |

## 稳定性验收

真实设备和长时间运行尚未执行，以下项目全部保持“待人工验证”：

| 场景 | 目标 | 状态 |
| --- | ---: | --- |
| Android 连播 | 100 首 | 待人工验证 |
| Windows 连播 | 100 首 | 待人工验证 |
| 原唱/伴唱切换 | 500 次 | 待人工验证 |
| 快速切歌 | 100 次 | 待人工验证 |
| 连续运行 | >= 4 小时 | 待人工验证 |
| Windows HDMI/电视、1080p/4K、DPI | 实机验证 | 待人工验证 |
| mpv.exe 崩溃后的恢复 | 实机验证 | 待人工验证 |

## NAS 数据安全最终检查

外部 NAS 只读安全不能仅靠 Release 配置宣称完成。已有 Backend 只读保护测试在本机可执行部分中通过；本轮没有连接真实 NAS，真实 NAS 只读验收仍待执行。

| 外部曲库危险操作 | 本轮本机运行计数 | 验收状态 |
| --- | ---: | --- |
| delete | 0（自动测试未触发） | 自动 guard 已通过；真实 NAS 待 CI/人工验收 |
| move | 0（自动测试未触发） | 自动 guard 已通过；真实 NAS 待 CI/人工验收 |
| rename | 0（自动测试未触发） | 自动 guard 已通过；真实 NAS 待 CI/人工验收 |
| overwrite/copy 覆盖 | 0（自动测试未触发） | 自动 guard 已通过；真实 NAS 待 CI/人工验收 |

人工验收时应使用本地复制的测试目录挂载为只读源目录，记录源文件路径、大小和 mtime 前后值，并在应用日志中确认外部模式未进入 delete、move、rename、覆盖或源转码路径；不得使用真实 NAS 做破坏性试验。
